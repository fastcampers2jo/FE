import { IcClose } from "assets";

const RankPop = (children: string) => (
  <div>
    <div>
      <span />
      {children}
      <button type="button">
        <IcClose />
      </button>
    </div>
  </div>
);

export default RankPop;
